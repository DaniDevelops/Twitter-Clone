export async function signup(req, res) {
  res.json({
    message: "You've hit the signin endpoint",
  });
}
export async function login(req, res) {
  res.json({
    message: "You've hit the login endpoint",
  });
}
export async function logout(req, res) {
  res.json({
    message: "You've hit the logout endpoint",
  });
}
